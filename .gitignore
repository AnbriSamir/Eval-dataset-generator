# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Env / secrets — never committed
.env
.env.*

# Local Claude Code settings (pedagogy hook wiring lives here, stays local)
.claude/settings.local.json

# Pipeline outputs (regenerated; provenance meta.json travels with its run, not git)
data/out/
data/runs/

# Inter-agent handoff reports (workflow-local, never committed)
.workflow-handoff/

# OS noise
.DS_Store
Thumbs.db
